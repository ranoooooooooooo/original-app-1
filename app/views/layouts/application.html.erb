<!DOCTYPE html>
<html>
  <head>  
    <title>Calen-Diary</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("logo.png", class: :logo), root_path %>
          </div>
          <% if user_signed_in? %>
            <div class="nav__right">
              <%= link_to "Log-Out", destroy_user_session_path, class: :nav__btn, data: { turbo_method: :delete } %>
              <%= link_to "New Room", new_room_path, class: :nav__btn %>
            </div>
          <% else %>
            <div class="nav__right">
              <%= link_to "Log-In", new_user_session_path, class: :nav__btn %>
              <%= link_to "New User", new_user_registration_path, class: :nav__btn %>
            </div>
          <% end %>
        </div>
      </div>
    </header>

    <%= yield %>
    
    <footer class="footer">
      <p class="copyright">Copyright © Calen-Diary</p>
    </footer>
  </body>
</html>
